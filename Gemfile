# Mirrors the exact gem set GitHub Pages builds with, so Vercel and Render
# produce a byte-identical _site. GitHub Pages itself ignores this file.
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
